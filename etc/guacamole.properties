# Guacd connection
guacd-hostname: guacd
guacd-port: 4822

# PostgreSQL configuration
postgresql-hostname: postgres
postgresql-port: 5432
postgresql-database: guacamole_db
postgresql-username: guacamole_user
postgresql-password: guacamole_password

# 设置动态连接认证为最高优先级
auth-provider: org.apache.guacamole.dynamic.DynamicConnectionAuthenticationProvider

# 如果需要保留 PostgreSQL 认证，可以注释掉或者放在后面
# auth-provider: org.apache.guacamole.auth.postgresql.PostgreSQLAuthenticationProvider

# 启用参数认证
enable-parameter-auth: true

# Log level for debugging
log-level: debug

# Disable environment properties to avoid conflicts
enable-environment-properties: false